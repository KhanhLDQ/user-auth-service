spring:
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        show_sql: false
  flyway:
    enabled: false

logging:
  level:
    org.hibernate.orm.jdbc.bind: info

email:
  verification:
    expiry: 10 #minutes
  sender: test@gmail.com

app:
  base-url: http://localhost